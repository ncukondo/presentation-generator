{
  "name": "presentation-generator",
  "module": "generate.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "bun run generate.ts",
    "screenshot": "bun run screenshot.ts",
    "build": "bun run generate && bun run screenshot",
    "tts": "bun run tts.ts",
    "video": "bun run video.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@mdi/svg": "^7.4.47",
    "jose": "^6.0.11",
    "pptxgenjs": "^4.0.1",
    "yaml": "^2"
  }
}
